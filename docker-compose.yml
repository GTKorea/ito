services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ito
      POSTGRES_PASSWORD: ito_dev
      POSTGRES_DB: ito
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
